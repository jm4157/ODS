// A conditional statement that checks if a screen transition can happen


/// @function              get_cdtn_trans(cdtn);
/// @param {string}  cdtn  Condition to check
/// @desc                  Checks if a screen transition is legal

function get_cdtn_trans(cdtn){
	switch(cdtn){
		default: return false;
	}
}
